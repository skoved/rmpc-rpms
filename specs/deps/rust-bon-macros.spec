# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate bon-macros

Name:           rust-bon-macros
Version:        3.3.2
Release:        %autorelease
Summary:        Proc-macro crate that is supposed to be a private implementation detail of the bon crate

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/bon-macros
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
This is a proc-macro crate that is supposed to be a private
implementation detail of the `bon` crate.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
# FIXME: no license files detected
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+experimental-getter-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+experimental-getter-devel %{_description}

This package contains library source intended for building other packages which
use the "experimental-getter" feature of the "%{crate}" crate.

%files       -n %{name}+experimental-getter-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+experimental-overwritable-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+experimental-overwritable-devel %{_description}

This package contains library source intended for building other packages which
use the "experimental-overwritable" feature of the "%{crate}" crate.

%files       -n %{name}+experimental-overwritable-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+implied-bounds-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+implied-bounds-devel %{_description}

This package contains library source intended for building other packages which
use the "implied-bounds" feature of the "%{crate}" crate.

%files       -n %{name}+implied-bounds-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires -a 

%build
%cargo_build -a 

%install
%cargo_install -a 

%if %{with check}
%check
%cargo_test -a
%endif

%changelog
%autochangelog
